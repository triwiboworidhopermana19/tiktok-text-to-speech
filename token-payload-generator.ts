import fs from 'fs';import moment from 'moment-timezone';import { Encryptor, LicensePayload } from './lib/license-checker';var privateKey = fs.readFileSync('./id_rsa', 'utf8');var encryptor: Encryptor = new Encryptor(privateKey);var payload: LicensePayload = {timezone: 'Asia/Jakarta',expiredAt: moment().tz('Asia/Jakarta').add(50, 'years').toDate()};var signature = encryptor.encrypt(payload);console.log(signature); 
